/**
 * Re-exported from @leanix-mock/shared so the meta model definition has a single source of
 * truth shared between the Prisma seed script and the running API (see packages/prisma/seed.ts).
 */
export * from '@leanix-mock/shared';
