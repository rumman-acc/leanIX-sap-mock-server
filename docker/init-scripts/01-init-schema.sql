-- Schema is managed entirely by Prisma migrations (packages/prisma/migrations), applied via
-- `prisma migrate deploy` when the api container starts (see docker-compose.yml). This script
-- only guarantees the database/role exist with the expected name before that runs.
SELECT 'init-scripts placeholder: schema managed by Prisma migrations';
